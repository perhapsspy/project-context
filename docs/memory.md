# 현재 메모리: project-context

- 업데이트 시각: 2026-03-19T02:00:00Z
- 업데이트 주체: ai

## 현재 초점
- `memory`는 현재 전역 상태만 담고, 다른 canonical surface가 소유한 상수 규칙을 다시 적지 않는다는 경계를 유지한다.

## 활성 결정
- 메모리 기록은 현재 사용자 주 언어로 작성한다.
- `docs/memory.md`는 현재 전역 상태만 담고, 다른 canonical surface가 이미 소유한 상수 규칙은 다시 적지 않는다.
- 현재 반복되는 drift는 migration 결과가 `memory`를 두 번째 instruction surface처럼 만들 때 생긴다.
- migration workflow inventory는 common context directories와 repo-root instruction files를 함께 본다.

## 참조 포인터
- docs/reference/model/context-surfaces.md
