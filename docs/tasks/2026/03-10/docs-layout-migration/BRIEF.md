# Docs Layout Migration

## Goal
- `docs/memory/` 트리를 `docs/memory.md`, `docs/reference/`, `docs/tasks/` 구조로 재배치하고 skill/guardrail/example 문서를 새 경로에 맞춘다.

## Scope
- skill 계약, guardrail, repo-local tests, dogfood/example memory 문서의 경로 일관성 정리
- 공개 전 스냅샷 기준으로 stale path 문구 정리

## Notes
- 이번 작업은 pre-publication cleanup이므로 기존 `docs/memory/` 이력 보존보다 최종 공개 구조 정합성을 우선한다.
