# Brief Thin Defaults

## Goal
- `BRIEF.md`가 다시 친절한 종합 메모로 두꺼워지지 않게 기본 작성 기준과 live example을 더 얇게 고정한다.

## Scope
- `BRIEF` 기본 예산과 optional heading 사용 기준을 더 분명히 한다.
- 설계성 task도 상세를 `BRIEF` 밖으로 빼는 규칙을 정리한다.
- representative `BRIEF` example을 실제로 더 얇게 다시 쓴다.

## Current Understanding
- 현재 코어 계약은 이미 얇은 `BRIEF`를 요구하지만, optional heading과 두꺼운 live example이 작성 습관을 다시 넓히고 있다.
- 재발 방지는 새 surface 추가보다 작성 컷라인과 representative example 정리가 더 직접적이다.
- 설계성 task라고 해서 `BRIEF`를 완화하면 다시 handoff memo 쪽으로 미끄러지기 쉽다.

## Current State
- status는 completed다.
- shipped `project-context`/`project-context-migration`는 `BRIEF`를 얇게 유지하는 기준을 더 짧게 정리하도록 갱신됐다.
- local direction, review, dogfood 문서도 같은 판정선으로 맞췄다.
- representative `BRIEF` example 3건은 optional heading을 걷어내고 현재 상태 중심으로 더 얇게 다시 썼다.
- 새 heuristic gardening rule은 넣지 않았다. 현재 evidence로는 noise 위험이 더 크다고 봤다.
- checks는 통과했고 gardening에는 기존 long-running task 1건만 info로 남는다.

## Next Step
- 없음
