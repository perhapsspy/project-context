**2026-03-13**
- 배경: README와 로컬 방향 문서는 `project-context`를 작업 맥락을 저장소 안에 남기는 스킬로 더 분명하게 설명하게 됐지만, shipped skill의 frontmatter와 intro는 여전히 `session recovery`와 `project memory` 쪽으로 먼저 읽혔다.
- 선택지: 1) README만 조정하고 shipped skill은 그대로 둔다, 2) contract 본문까지 재작성한다, 3) shipped skill의 frontmatter와 intro만 좁게 조정해 첫인상만 맞춘다.
- 결정: contract와 workflow는 유지하고, `skills/project-context/SKILL.md`와 `skills/project-context-migration/SKILL.md`의 frontmatter와 intro만 새 framing에 맞춰 조정한다.
- 영향: user-facing 설명 surface가 더 일관돼지고, 스킬의 실제 운영 계약은 그대로 유지된다.
